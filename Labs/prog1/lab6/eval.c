#include<math.h>

/* 
 * Calculates a third-degree polynomial.
 * Parameter: x, indeterminate
 * Returns: the value of the polynomial for a given x.
 */
//double eval (double x) {
//	return pow(x, 3)+2*pow(x,2);
//}


// enallaktiki ulopoiisi
double eval (double x) {
	return cos(x);
}

